# Notas:

```
Options:
--help     Show help                                             [boolean]
--version  Show version number                                   [boolean]
-b, --base     Es la base de la tabla de multiplicar       [number] [required]
-l, --listar   Indica si se va a listar en consola los resultados de la tabla
[boolean] [default: false]
-h, --hasta    Indica hasta que numero se va a multiplicar la base
[number] [default: 10]
```
